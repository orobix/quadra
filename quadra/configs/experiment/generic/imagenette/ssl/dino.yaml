# @package _global_

defaults:
  - base/ssl/dino
  - override /datamodule: generic/imagenette/ssl/base

core:
  tag: "run"
  name: "dino"

logger:
  mlflow:
    experiment_name: imagenette_ssl_new
    run_name: ${core.name}

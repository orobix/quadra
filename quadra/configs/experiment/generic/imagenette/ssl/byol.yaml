# @package _global_

defaults:
  - base/ssl/byol
  - override /datamodule: generic/imagenette/ssl/base

core:
  tag: "run"
  name: "byol"

logger:
  mlflow:
    experiment_name: imagenette_ssl_new
    run_name: ${core.name}
